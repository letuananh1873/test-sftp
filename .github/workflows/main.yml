name: 🚀 Deploy website on push sFTP

on:
  push:
    branches: [master]

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: 128.199.207.121
        username: skuatstaging
        password: ${{ secrets.password }}
        server-dir: /public_html/wp-content/themes/test-sftp/
        protocol: ftps
        port: 22
